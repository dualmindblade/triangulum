[package]
name = "triangulum-multiplayer"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Headless multiplayer protocol and state core for Triangulum"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
